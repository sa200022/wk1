# ============================================================================
# Environment Variables Template for Webhook Delivery System
# ============================================================================
# Usage:
#   1. Copy this file to .env
#   2. Replace all passwords with strong secrets
#   3. Never commit .env to version control (ensure it's in .gitignore)
# ============================================================================

# MySQL Root Password
MYSQL_ROOT_PASSWORD=root_password_change_me_in_production

# Database User Passwords (match with 002_DatabaseRoles_Development.sql)
DB_PASSWORD_EVENT_INGEST=dev_password_event_ingest
DB_PASSWORD_ROUTER_WORKER=dev_password_router
DB_PASSWORD_SAGA_ORCHESTRATOR=dev_password_orchestrator
DB_PASSWORD_JOB_WORKER=dev_password_worker
DB_PASSWORD_DEAD_LETTER_OPERATOR=dev_password_deadletter
DB_PASSWORD_SUBSCRIPTION_ADMIN=dev_password_subscription

# ============================================================================
# Production Deployment Notes:
# ============================================================================
# 1. Generate strong passwords (at least 32 characters, random)
# 2. Store secrets in a vault (AWS Secrets Manager, HashiCorp Vault, etc.)
# 3. Use Docker secrets or Kubernetes secrets for production
# 4. Enable SSL/TLS for MySQL connections (update connection strings)
# 5. Rotate passwords regularly (every 90 days)
# ============================================================================
